package spec

import (
	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

var _ = Describe("Spec", func() {

	// Decide which crypto-sdk functionality should be tested on e2e.
	// Call each flow once, this is not the stress test

	It("should create address", func () {

		Expect(true)


	})


})
